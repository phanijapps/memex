# workspace.toml
#
# Repository coordination index. Canonical artifacts own requirements; this
# file records lifecycle membership, source provenance, display summaries, and
# hard dependencies.
#
# Target entries are inline tables with exactly:
#   path, kind, source, summary, needs
#
# Example:
#   { path = "docs/specs/<slug>/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Example spec", needs = [] }
#
# Comments, summaries, list order, tracker labels, and profile hints are
# non-semantic. They must not determine routing, dependency satisfaction,
# processor selection, or dispatch.
#
# Paths are repository-relative POSIX paths. Consumers must reject absolute
# paths, backslashes, ".." segments, and any symlink-resolved target outside the
# repository root.

[backlog]
open = [
  { path = "docs/intent-continuity-survey.md", kind = "research", source = { mode = "repo-origin", ref = "docs/intent-continuity-survey.md", revision = "2026-09-20" }, summary = "Evaluate applicable task intent and continuity across coding-agent sessions", needs = [] },
  { path = "docs/backlog/003-link-expansion-eval.md", kind = "research", source = { mode = "repo-origin", parent = "docs/specs/link-expansion/spec.md" }, summary = "Measure link expansion on a linked fixture with the existing six-metric rubric plus attribution and an inbound-expansion arm", needs = [] },
]
closed = []

["ini-001"]
name      = "Memory retrieval and evidence quality"
status    = "active"
milestone = "Evidence-aware retrieval"

["ini-001".work]
queue   = [
  { path = "docs/specs/project-concept-types/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "none" }, summary = "Declare project-level concept types beyond the built-in five, with the directory as the declaration", needs = [] },
  { path = "docs/specs/link-graph-visualizer/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "none" }, summary = "D3 force-directed view of the typed link graph in the local dashboard, with governance verbs on draft types", needs = ["docs/specs/memory-governance/spec.md"] },
  { path = "docs/specs/memory-governance/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/specs/project-concept-types/spec.md" }, summary = "Approval lifecycle for pending pages and draft types: human verbs on CLI and viz, policy-bounded agent approval over MCP, log.md history", needs = ["docs/specs/project-concept-types/spec.md"] },
  { path = "docs/specs/git-backed-memory/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/v1_enhance.md" }, summary = "Store as a git repository: approvals become branches and merge requests, cold-start seeding from project history, docs/ only ever versioned", needs = ["docs/specs/memory-governance/spec.md"] },
  { path = "docs/specs/project-aware-injection/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/product/briefs/intent-continuity.md" }, summary = "Measure session-start relevance and conditionally select project-aware memories", needs = [] },
]
active  = [
  { path = "docs/specs/okf-frontmatter/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "none" }, summary = "Make page front matter a conformant OKF v0.2 concept, OKF fields first", needs = [] },
  { path = "docs/specs/task-evidence-recall/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/product/briefs/intent-continuity.md" }, summary = "Evaluate and guide focused task recall using existing Memex operations", needs = [] },
  { path = "docs/specs/quality-gated-retrieval/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/product/briefs/memory-retrieval-and-evidence.md" }, summary = "Select and ship one lexical ranker that improves hard-query accuracy and context cost within strict latency gates", needs = [] },
  { path = "docs/specs/agentic-frontmatter-search/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/product/briefs/memory-retrieval-and-evidence.md" }, summary = "Add searchable page descriptions, generated directory indexes, and a bounded search-read-follow agent journey", needs = [] },
  { path = "docs/specs/incremental-navigation/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "none" }, summary = "Single-page navigation refresh splices the page's row into index.md (O(1) file opens, bytes equal full render); 354 ms -> 14 ms per write at 600 pages.", needs = [] },
  { path = "docs/specs/link-expansion/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "none" }, summary = "Link-graph expansion (OKF read_concept) appends one-hop linked pages after direct hits in session-start and task recall within the same token budget", needs = [] },
  { path = "docs/specs/navigation-search/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "none" }, summary = "navigation-search: dependency-free recall over generated index.md rows via Memex.recall(engine=\"navigation\") and memex recall --engine, with automatic fallback when the SQLite index has zero rows.", needs = [] },
]
shipped = [
  { path = "docs/specs/project-scoped-memory-migration/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/product/briefs/project-scoped-memory.md" }, summary = "Add project-scoped pages, scoped recall, and dated transcript lifecycle without runtime migration", needs = [] },
  { path = "docs/specs/dashboard-memory-explorer/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/product/briefs/modern-htmx-dashboard.md" }, summary = "Paginate scoped memories and render grouped session replay in the local dashboard", needs = [] },
  { path = "docs/specs/readable-project-folders/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "none" }, summary = "Store project memory in readable Git or folder directories without merging distinct project IDs", needs = [] },
]

["ini-001".shaping_queue]
active  = []
backlog = []

["ini-001".brief_queue]
draft = []
ready = [
  { path = "docs/product/briefs/intent-continuity.md", kind = "brief", source = { mode = "repo-origin", ref = "docs/intent-continuity-survey.md", revision = "2026-09-20" }, summary = "Recover complete, relevant coding-task evidence within a bounded context", needs = [] },
  { path = "docs/product/briefs/memory-retrieval-and-evidence.md", kind = "brief", source = { mode = "repo-origin" }, summary = "Improve retrieval ranking, context efficiency, temporal evidence, and consolidation safety", needs = [] },
  { path = "docs/product/briefs/modern-htmx-dashboard.md", kind = "brief", source = { mode = "repo-origin" }, summary = "Revamp Memex into a modern, dashboard-first HTMX experience", needs = [] },
]
executing = []
shipped   = [
  { path = "docs/product/briefs/project-scoped-memory.md", kind = "brief", source = { mode = "repo-origin" }, summary = "Organize memory by project with project-level and global search", needs = [] },
]
withdrawn = []
cancelled = []
